# Scientra Copilot Dependencies
# Generated from verified working installation (Python 3.11+)

# Core
fastapi>=0.100
uvicorn>=0.23
typer>=0.9
pydantic>=2.0
pyyaml>=6.0
loguru>=0.7
requests>=2.28

# Vector & Embedding
lancedb>=0.4
pyarrow>=12.0
sentence-transformers>=2.2
torch>=2.0
transformers>=4.30
huggingface-hub>=0.16
numpy>=1.24

# ── Hybrid PDF Parser (optional — install via Scripts/setup_parsers.py) ──
# PyMuPDF: PDF scanning + figure extraction
pymupdf>=1.24
# OpenDataLoader PDF: markdown + layout + tables (requires Java 11+)
opendataloader-pdf>=2.4
# Marker: high-quality markdown fallback (requires PyTorch, surya-ocr)
# marker-pdf>=1.10
# surya-ocr>=0.17  (auto-installed with marker-pdf)

# Optional: GROBID Docker management
# docker>=6.0

# Development
# pytest>=7.0
# pytest-asyncio>=0.21
# ruff>=0.1
